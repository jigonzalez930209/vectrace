# Overlay Modes & System Tray

## Click-Through Mode (Passthrough)
Toggle mouse passthrough with `Space` or `Ctrl+Alt+A`. When enabled, mouse events pass through the transparent canvas to underlying apps while keeping your drawings visible.

## System Tray Integration
Vectrace runs as a background daemon with system tray controls for quick access to tools, background modes, and monitor selection.
